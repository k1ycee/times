import 'package:flutter/material.dart';

class Loads extends StatefulWidget {
  @override
  _LoadsState createState() => _LoadsState();
}

class _LoadsState extends State<Loads> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Loading Times'),
    );
  }
}
